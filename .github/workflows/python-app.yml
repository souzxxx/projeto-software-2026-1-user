<<<<<<< HEAD
name: Deploy Automático Python Flask
=======

name: Python application
>>>>>>> refs/remotes/origin/main

on:
  push:
    branches: [ "main" ]

jobs:
  build:
<<<<<<< HEAD
    runs-on: ubuntu-latest
    steps:
    - name: Checkout código
      uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"
        cache: 'pip'

    - name: Instalar dependências
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: "souzx"
=======

    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
        
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: "ezambomsantana"
>>>>>>> refs/remotes/origin/main
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        push: true
<<<<<<< HEAD
        tags: souzx/users-ci:${{ github.sha }}

    - name: Deploy via SSH
=======
        tags: ezambomsantana/users-ci:${{ github.sha }}
        
    - name: executing remote ssh commands using password
>>>>>>> refs/remotes/origin/main
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST_TEST }}
        username: "ubuntu"
        key: ${{ secrets.KEY_TEST }}
        port: 22
        script: |
<<<<<<< HEAD
          docker stop users || true
          docker rm --force users || true
          docker image prune -f
          
          # Injeção das 3 variáveis que seu main.py exige
          docker run -d \
            -p 80:5000 \
            -e POSTGRES_USER="${{ secrets.POSTGRES_USER }}" \
            -e POSTGRES_PASSWORD="${{ secrets.POSTGRES_PASSWORD }}" \
            -e POSTGRES_URL="${{ secrets.POSTGRES_URL }}" \
            -e REDIS_URL=redis://redis:6379 \
            --network=rede \
            --name users \
            souzx/users-ci:${{ github.sha }}
=======
          docker stop users
          docker rm --force users
          docker run -d --name users -e POSTGRES_URL=postgres-users -e POSTGRES_USER=${{ secrets.POSTGRES_USER }} -e POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }} --network=rede -p 80:5000 ezambomsantana/users-ci:${{ github.sha }}


>>>>>>> refs/remotes/origin/main
